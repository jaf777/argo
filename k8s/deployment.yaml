apiVersion: apps/v1
kind: Deployment
metadata:
  name: hola-mundo-node
  labels:
    app: hola-mundo-node
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hola-mundo-node
  template:
    metadata:
      labels:
        app: hola-mundo-node
    spec:
      containers:
      - name: hola-mundo-node
        image: jaf777/hola-mundo-node:1.0
        ports:
        - containerPort: 3000
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
